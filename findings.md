The biology engine supports GenBank/FASTA/JSON and shared ConstructRef mutations.
Bootstrap currently requires both adapters; native desktop access is unavailable.
Explicit overlap lengths avoid silently choosing ambiguous repeat junctions.
Assembly retains both copies of source annotations if sources independently annotate a shared overlap.
Circular origin mapping preserves positive- and negative-strand feature sequences in GenBank round trips.
FastMCP converts structured fragment dictionaries to ConstructRef successfully.
